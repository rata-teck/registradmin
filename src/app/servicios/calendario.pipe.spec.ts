import { CalendarioPipe } from './calendario.pipe';

describe('CalendarioPipe', () => {
  it('create an instance', () => {
    const pipe = new CalendarioPipe();
    expect(pipe).toBeTruthy();
  });
});
